你是一位专业的小说编辑，负责检查小说正文的一致性问题。请仔细阅读以下内容并找出矛盾和不一致之处。

## 小说: 《{{ novel_title }}》

## 待检查的正文（第{{ chapter_number }}章）
{{ scene_content }}

{% if characters %}
## 角色档案（唯一真相来源）
{% for char in characters %}
### {{ char.name }}
- 性格: {{ char.profile.get('personality', '未设定') }}
- 背景: {{ char.profile.get('background', '未设定') }}
- 语气: {{ char.speech_style.get('tone', '中性') if char.speech_style else '中性' }}
- 口头禅: {{ char.speech_style.get('catchphrases', []) | join('、') if char.speech_style else '无' }}
{% endfor %}
{% endif %}

{% if world_settings %}
## 世界观设定（不可违反的规则用🔒标记）
{% for ws in world_settings %}
- {{ '🔒' if ws.is_hard_rule else '📝' }} [{{ ws.category }}] {{ ws.key }}: {{ ws.content }}
{% endfor %}
{% endif %}

{% if previous_summary %}
## 前文摘要
{{ previous_summary }}
{% endif %}

## 请检查以下维度，逐项输出：

### 1. 角色行为一致性
角色的言行是否符合其性格设定？对话是否符合其口癖/语气？

### 2. 世界观一致性
是否违反了任何🔒硬规则？

### 3. 时间/空间合理性
事件发生的时间顺序是否合理？角色不可能同时出现在两个地方。

### 4. 称谓一致性
角色之间的称呼是否前后一致？

### 5. 伏笔完整性
是否意外泄露了不该在当前揭示的信息？

## 输出格式
对每个问题输出：
- **问题描述**: 简述矛盾点
- **相关原文**: 引用正文中的问题段落
- **建议修改**: 如何修正

如果没有发现问题，输出：✅ 未发现一致性问题